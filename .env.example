# Public base URL of the deployed site, used for absolute Open Graph/Twitter
# image URLs and canonical metadata. Set this to your production domain
# (e.g. https://cobalt.example.com) in your deployment environment.
# Falls back to http://localhost:3000 if unset.
NEXT_PUBLIC_SITE_URL=
